# Configuration examples

- [`homebridge-config.json`](homebridge-config.json) — minimal `platforms[]` block with every option set to its default. Drop into your Homebridge `config.json` and tweak `url` / `password`.
